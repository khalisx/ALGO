#ifndef UTILS_H
#define UTILS_H

// Convertit une chaîne de caractères en minuscules
void toLowerCase(char *str);

// Normalise une chaîne en supprimant les accents et en convertissant les caractères en minuscules
void normaliserChaine(const char *chaineOrigine, char *chaineNormalisee);

#endif
